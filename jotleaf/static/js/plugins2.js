// Generated by CoffeeScript 1.4.0

jQuery.fn.br2nl = function() {
  return this.each(function() {
    return $(this).children('br').after('\n');
  });
};

jQuery.fn.findOne = function(selector) {
  var result;
  result = this.find(selector);
  if (result.length !== 1) {
    jQuery.error("Didn't find one " + selector);
  }
  return this.pushStack(result);
};

$.fn.colorpicker.Constructor.prototype.place = function() {
  var inputHeight, offset, pickerHeight, positionAbove, positionBelow, topOffset, winHeight;
  if (this.component) {
    offset = this.component.offset();
  } else {
    offset = this.element.offset();
  }
  winHeight = $(window).height();
  pickerHeight = this.picker.outerHeight();
  inputHeight = this.height;
  positionAbove = offset.top - pickerHeight;
  positionBelow = offset.top + inputHeight;
  if ((positionBelow + pickerHeight) <= winHeight) {
    topOffset = positionBelow;
  } else {
    topOffset = positionAbove;
  }
  return this.picker.css({
    top: topOffset,
    left: offset.left
  });
};
